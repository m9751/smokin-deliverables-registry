# Ignore list — OPERATOR-CONTROLLED ONLY.
# The sweep does NOT auto-add anything here. Nothing is screened out unless
# Michael explicitly says so. Empty = everything discovered becomes a candidate.

# ── Seeded 2026-06-23: skipped buckets (operator-approved cut) ──
# Registry holds live websites only (host: vercel | github-pages). Skipped:
#   ~199 github.com commit/PR/file links — git history/provenance, not a deliverable page; no schema slot.
#   ~20  local paths (file://, /Users/, C:\) — not findable by anyone.
#   ~11  db links (supabase://, qdrant://, kb://) — data, not a hostable page.
#   ~19  external/auth-walled (figma, notebooklm, hindsight, skills.sh, misc http) — not m9751-hosted.
# Filed: 26 new unique live sites (vercel + github-pages) as status:draft, plus pre-existing harness-resource-hub.
